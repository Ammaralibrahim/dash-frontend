// pages/login.tsx
import React from 'react';
import {Login} from '../components/login/page';

const LoginPage = () => {
   return <Login />;
};

LoginPage.noLayout = true; // Layout'u devre dışı bırak

export default LoginPage;
